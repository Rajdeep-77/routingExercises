import { SpeicalItemDirective } from './speical-item.directive';

describe('SpeicalItemDirective', () => {
  it('should create an instance', () => {
    const directive = new SpeicalItemDirective();
    expect(directive).toBeTruthy();
  });
});
